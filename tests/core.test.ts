import {describe,it,expect} from "vitest";import{heuristicEstimate,calibrated}from"@/lib/estimate";import{urgency}from"@/lib/urgency";import{plan}from"@/lib/planner";import{transition}from"@/lib/state-machine";
describe("estimation",()=>{it("recognizes mounting and quantities",()=>expect(heuristicEstimate("Mount 4 security cameras").minutes).toBe(155));it("uses calibration after five samples",()=>expect(calibrated(60,1.5,5)).toBe(90));it("does not calibrate too early",()=>expect(calibrated(60,1.5,4)).toBe(60));});
describe("priority",()=>{it("ranks overdue work higher",()=>expect(urgency({priority:"MEDIUM",deadline:new Date(Date.now()-864e5),createdAt:new Date(),estimatedMinutes:30})).toBeGreaterThan(urgency({priority:"MEDIUM",deadline:null,createdAt:new Date(),estimatedMinutes:30})));});
describe("planner",()=>{it("never exceeds a session",()=>{const now=new Date();const r=plan([{priority:"HIGH" as const,deadline:null,createdAt:now,estimatedMinutes:45},{priority:"LOW" as const,deadline:null,createdAt:now,estimatedMinutes:30}],60);expect(r.totalMinutes).toBe(45)});});
describe("state machine",()=>{it("enforces lifecycle",()=>{expect(transition("OPEN","start")).toBe("IN_PROGRESS");expect(transition("IN_PROGRESS","complete")).toBe("COMPLETED");expect(()=>transition("OPEN","complete")).toThrow()});});
