import{prisma}from"@/lib/prisma";export async function GET(){const rows=await prisma.task.findMany({where:{status:"COMPLETED"},include:{category:true,completedBy:true},orderBy:{completedAt:"desc"}});const esc=(v:unknown)=>`"${String(v??"").replaceAll('"','""')}"`;const text=[["title","category","completer","completed date","estimated minutes","actual minutes"],...rows.map(t=>[t.title,t.category?.name,t.completedBy?.name||t.completedBy?.email,t.completedAt?.toISOString(),t.estimatedMinutes,t.actualMinutes])].map(r=>r.map(esc).join(",")).join("\n");return new Response(text,{headers:{"content-type":"text/csv","content-disposition":"attachment; filename=officeops-archive.csv"}})}
