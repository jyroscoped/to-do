export type Estimate={minutes:number;confidence:number;rationale:string;source:"HEURISTIC"};
const rules:[RegExp,number,string][]=[[/\b(mount|hang|install).*(tv|monitor|camera|shelf)|\b(tv|monitor|camera|shelf).*(mount|hang|install)/i,45,"Mounting, alignment, setup, and cleanup."],[/\b(assemble|build).*(desk|stand|chair|furniture)|\b(desk|stand|chair|furniture).*(assemble|build)/i,60,"Assembly and cleanup."],[/\b(trace|cable|wire|network|ethernet|starlink|router)\b/i,60,"Tracing and testing cables or network equipment."],[/\b(configure|set ?up|setup).*(appliance|device|printer|coffee)|\b(appliance|device|printer|coffee).*(configure|set ?up|setup)/i,30,"Configuration and testing."],[/\b(clean|organize|restock|tidy)\b/i,20,"Cleaning or organizing work."],[/\b(buy|order|purchase|pick up)\b/i,15,"Procurement or pickup."],[/\b(move|carry|deliver)\b/i,15,"Moving or delivery."],[/\b(event|usher|greet)\b/i,120,"Event support time."]];
export function heuristicEstimate(title:string,description=""):Estimate { const text=`${title} ${description}`; let found=rules.find(([rx])=>rx.test(text)); let minutes=found?.[1]??30; const amount=text.match(/(?:×|x)\s?(\d+)|\b(\d+)\s+(?:cameras|monitors|chairs|desks)\b|\b(two|three|four|five)\b/i); const word:{[k:string]:number}={two:2,three:3,four:4,five:5}; const n=amount?Number(amount[1]||amount[2]||word[amount[3]?.toLowerCase()]||1):1; if(n>1) minutes*=1+.8*(n-1); if(/\b(quick|just|small)\b/i.test(text)) minutes*=.6; if(/\b(all|entire|whole office)\b/i.test(text)) minutes*=2; minutes=Math.min(480,Math.max(5,Math.round(minutes/5)*5)); return {minutes,confidence:.35,rationale:found?.[2]??"Typical office operations task." ,source:"HEURISTIC"}; }
export const calibrated=(base:number,ratio:number,samples:number)=>Math.round(Math.min(480,Math.max(5,base*(samples>=5?Math.min(2,Math.max(.5,ratio)):1))/5))*5;
